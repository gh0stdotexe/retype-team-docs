To create a backup of the entire data directory for a chain, simply use the following command to create a tarball:

```shell
# stop your node first!
tar czf juno_snapshot.tar.gz -C ~/.juno/data .
```

<br>
